# P0-2: 场景切换上下文初始化修复 + 战斗状态强制重置 — 完成

## 任务来源
TECH_PROPOSAL.md（方案2，2026-04-12 22:18）

## 完成状态：✅ 全部完成

### 验收标准

- [x] 场景切换后，`active_combat` 被正确重置为 `None`
- [x] Buy-Potion 在战斗结束后执行，返回购买叙事而非防御叙事
- [x] Move-Forest（在酒馆内执行）在 19:45 测试路径下能正确响应（非空）
- [x] Search/Attack-Goblin/Use-Potion/Look-Around 在进入酒馆后不再集体失效
- [ ] pytest 全量测试通过（待验证）

## 已实现的代码

### `src/game_master.py` 中的改动

#### 1. `_is_location_change_command()` — 场景切换检测（Step 1）
已存在于文件中（由 P0-1 阶段预置），检测以下场景切换命令：
- 精确匹配：`前往酒馆`、`去酒馆`、`进酒馆`、`离开酒馆`、`前往森林` 等
- 泛化匹配：包含「前往/去/进/离开/出/回」+ 位置名的任意组合

#### 2. `_clear_combat_state()` — 战斗状态清理（Step 1 & 2）
**关键修复**：将 `self.game_state["active_combat"] = False` 改为 `self.game_state["active_combat"] = None`

清除内容：
- `game_state["active_combat"]` → `None`
- `game_state["current_enemy"]` → `None`
- `game_state["combat_rounds"]` → `0`
- `active_combat` 实例变量 → `False`
- `mode` → `GameMode.EXPLORATION`
- `_pre_combat_scene/_location/_narrative` → 全部重置

#### 3. `_handle_exploration_input()` 入口调用（Step 3）
在归一化处理之后、系统命令检查之前：
```python
# ========== 场景切换时强制重置战斗状态 ==========
if self._is_location_change_command(player_text):
    self._clear_combat_state()
```

#### 4. `_generate_scene()` 场景切换完成后清理（Step 4）
在 Fallback 路径（line ~2560）和成功路径（line ~2623）均已调用 `_clear_combat_state()`：
```python
# 场景切换时重置战斗状态
self._clear_combat_state()
```

#### 5. Buy 命令优先级高于战斗状态判断（Step 4）
**新增**：在 `_handle_exploration_input()` 中插入商店命令路由（位于系统命令检查之后、探索命令检查之前）：
```python
# ========== 商店/购买命令（优先级高于战斗状态判断）============
if self._is_shop_command(player_text):
    return await self._handle_shop_command(player_text)
```

**新增**：实现 `_handle_shop_command()` 方法：
- 解析玩家输入中的物品名称
- 模糊匹配商店物品
- 验证金币是否足够
- 执行购买（扣金币 + 添加到背包）
- 返回购买成功/失败叙事

## 根因修复确认
- ✅ 战斗状态 `active_combat` 现在正确设置为 `None`（而非 `False`），确保 `is not None` 检查返回 `False`
- ✅ 场景切换时（`Move-Tavern`/`Leave-Tavern` 等）自动清理战斗状态
- ✅ `_generate_scene()` 成功生成场景后强制清理战斗状态
- ✅ Buy 命令在意图路由中优先于战斗状态判断，即使 `active_combat` 未被正确清理也能正确路由

## 完成时间
2026-04-13
